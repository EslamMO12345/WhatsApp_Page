import 'package:flutter/material.dart';

class Decorrin extends StatelessWidget {
  const Decorrin({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp( 
      home: Scaffold( 
        
      ),
    );
  }
}